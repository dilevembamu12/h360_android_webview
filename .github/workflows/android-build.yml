name: Android APK Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle 8.7 (no local Android Studio required)
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.7"

      - name: Build Debug APK
        run: gradle --no-daemon --stacktrace clean :app:assembleDebug

      - name: Build Release APK (unsigned)
        run: gradle --no-daemon --stacktrace :app:assembleRelease

      - name: Upload Debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: h360pos-webview-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error

      - name: Upload Release APK artifact (unsigned)
        uses: actions/upload-artifact@v4
        with:
          name: h360pos-webview-release-apk-unsigned
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error
